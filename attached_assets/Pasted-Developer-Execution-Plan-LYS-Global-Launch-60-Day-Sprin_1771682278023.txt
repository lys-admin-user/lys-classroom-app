Developer Execution Plan: LYS Global Launch (60-Day Sprint)Target Launch Date: [1-2 Months from Start]Status: High Priority / Production PathObjective: Deploy a secure, multi-tenant global MVP with a "Safety Suite" and Dynamic Pricing.1. Immediate Infrastructure Setup (Days 1–7)To meet the 60-day deadline, the engineering team must implement the following immediately:Cloud Provider: AWS (Primary). Use Terraform/IAC to define environments for US, EU, and Africa to handle Data Residency.Database: PostgreSQL with Row Level Security (RLS) enabled. This is non-negotiable for the "Apartment Building" model.Authentication: Implement Clerk or Auth0 with MFA. Do not build custom auth to save time.AI Gateway: Use LangChain to manage RAG (Retrieval-Augmented Generation) for the "Master Builder" AI, ensuring student PII is stripped before sending to LLMs.2. Technical Requirements: Security & Compliance2.1 The "Apartment Building" IsolationEvery table in the database must include a tenant_id (UUID).Middleware: Every API request must pass through a global middleware that extracts the tenant_id from the JWT and sets the Postgres session variable for RLS.Requirement: SELECT * FROM students; must automatically only return students belonging to that teacher's school.2.2 Safety Suite Review QueueLogic: Implement a "Shadow Table" for messages.Status Flag: Messages are marked pending_review by default if triggered by a keyword filter or user role.Admin UI: A "Review Queue" dashboard allowing District Admins to bulk-approve or flag messages for archiving.3. High-Velocity Feature Parity (Days 8–45)To launch in 2 months, we focus only on these core "Climb" features:FeatureTech ImplementationPriorityMaster Builder AIRAG using "Bricks" metadata as the context window.P0Success ScannerMobile-first web app using Tesseract.js or AWS Textract.P0Dynamic PricingMiddleware checking GeoIP against the World Bank API lookup.P1Affiliate DashboardSimple referral tracking linked to Stripe Connect.P14. Security & Compliance Checklist (The "Safe Climb")USA (COPPA/FERPA): Implement a "Parental Consent" barrier. If user.age < 13, lock the account until a parent_id is linked and verified.Nigeria (NDPR): Set up an AWS instance in the Cape Town region (or local Lagos node) specifically for data storage for the .ng TLD.Security Audit: Run an automated OWASP ZAP scan in week 6.5. Deployment & "Zero Hour" StrategyWeek 7: The "Beta Nest" Load TestSimulate 1,000 concurrent "Brick" assemblies.Test "VPN Guardrail" to ensure users cannot spoof locations for lower pricing.Week 8: Production RolloutDNS: Global routing via Cloudflare to handle regional data residency redirects.Support: Deploy the "AI Support Bot" pre-loaded with help documentation for the "Success Scanner."6. Developer Workstreams (Parallel Execution)WorkstreamLeadPrimary TaskBackendSenior DevRLS Logic & Tenant Isolation API.AI/DataML EngineerMaster Builder prompt engineering & RAG pipeline.Mobile/FrontLead FrontendSuccess Scanner UI & Offline-first data entry.DevOpsSREMulti-region AWS deployment & Compliance logging.