# LYS Educational Platform

LYS is an AI-powered educational platform that helps students and educators achieve academic and real-world success through personalized learning and comprehensive resources.

## Run & Operate

_Populate as you build_

## Stack

-   **Frontend**: React 18, TypeScript, Wouter, TanStack React Query, Vite
-   **Backend**: Express.js, TypeScript, Zod
-   **Database**: PostgreSQL, Drizzle ORM
-   **UI**: Tailwind CSS, shadcn/ui
-   **Authentication**: Replit Auth, `express-session`
-   **AI**: OpenAI API

## Where things live

-   **Monorepo Structure**: `/client` (frontend), `/server` (backend), `/shared` (common utilities)
-   **Developer Standards**: `docs/developer-execution-plan.md`
-   **Feature-Specific Architecture**: `docs/`
-   **PII Sanitization Logic**: `server/services/piiSanitizer.ts`
-   **OpenAI Integration**: `server/openai.ts`, `server/assignmentGenerator.ts`
-   **AI Lesson Plan Storage**: `server/storage/lessonAi.ts`
-   **LYS Reference Corpus**: `server/reference/lys/` (text files including `voice_master_teacher.txt` + `exemplar_*.txt`), `server/reference/lys/embedded.ts` (embedded output), `server/lysReference.ts` (API)
-   **Voice Infusion**: `server/services/voiceProfileService.ts`, `server/services/voiceCriticService.ts`
-   **Scholarship Scraper**: `server/scholarshipScraper/`
-   **Standards Ingestion (authoritative)**: `server/services/cspService.ts` (Common Standards Project sync — full/real data), `server/services/standardsCatalog.ts` (runtime cascade), `server/services/llmExtractionService.ts` (PDF/text extraction, chunked), routes in `server/routes/org.ts` (`/api/standards/*`)
-   **Standards Course Layer (Subject→Course→Codes)**: `shared/courseGrouping.ts` (pure engine: `cleanCourseName`/`cleanSubjectLabel`/`isGradeOnlyTitle`/`groupSetsIntoCourses`/`preferSet`) turns one umbrella subject (e.g. TX "Social Studies (2020-)") into the real courses a teacher picks (World Geography, US History...). `standardsCatalog.ts` exposes `listCourses` + a `courseId` branch in `listCodes`; endpoint `GET /api/standards/courses/:country/:state/:subject` + `?course=` on `/codes` & `/my-codes`. UI: `client/src/components/StandardsCascadePicker.tsx` shows the Course step only when a subject fans out to >1 course. Tests: `server/__tests__/course-grouping.test.ts`.
-   **Texas Agriculture TEKS Seed**: `server/services/agricultureTeksSeed.ts` (`seedTexasAgricultureTeks`) + `scripts/seed-texas-ag-teks.ts` (run `npx tsx scripts/seed-texas-ag-teks.ts`)
-   **Career Explorer Catalog (~830 careers)**: 42 hand-curated entries in `seedCareers` (`server/storage/_base.ts`, stable ids "1".."42" referenced by `saved_careers`) + ~800 auto-generated occupations in `server/storage/careersGenerated.ts` (ids `soc-<SOC>`), merged into `seedCareers` (deduped by `blsCode`/title). Regenerate the generated file with `npx tsx scripts/ingest-bls-careers.ts`. UI: `client/src/pages/Careers.tsx` (`categories` + `categoryLabels` keyed by SOC major group; `usStates` covers all 50 + DC).
-   **Database Schema**: Refer to Drizzle ORM schema files.
-   **Embedded Routes**: `client/src/pages/EmbedRouter.tsx`, `client/src/components/EmbedWrapper.tsx`, `client/src/components/EmbedSidebar.tsx`
-   **Global Command Palette (jump-to search)**: `client/src/components/CommandPalette.tsx` (reuses `navigationGroups` + `hasMinRole` exported from `client/src/components/AppSidebar.tsx`)
-   **Role-Aware Home Band**: `client/src/components/RoleQuickStart.tsx` (rendered atop the authenticated Dashboard)
-   **Anonymous Landing**: `client/src/components/RoleRoutedLanding.tsx` (full-screen role-routed pre-login landing; mounted via `AppShell` in `client/src/App.tsx`)
-   **Guest Email Gate**: `client/src/components/GuestEmailModal.tsx`, `guest_leads` table in `shared/schema.ts`, endpoints in `server/routes/lessons.ts`
-   **Student Practice Flow**: `client/src/pages/PracticeGenerator.tsx` (page, route `/practice`), `server/routes/practice.ts` (endpoints), `server/practiceGenerator.ts` (OpenAI generator), schema in `shared/schema.ts` (`generatePracticeRequestSchema`, `PracticeQuestion`, `GeneratedPracticeSet`)
-   **School Admin "See it for your school"**: `client/src/pages/ForSchools.tsx` (page, route `/for-schools`), `server/routes/demo.ts` (`POST /api/demo-requests`), `server/storage/demo.ts` (`createDemoRequest`), `demo_requests` table + `demoRequestSchema` in `shared/schema.ts`
-   **Homeschool Weekly Planner**: `client/src/pages/HomeschoolPlanner.tsx` (page, route `/homeschool`), `server/routes/homeschool.ts` (endpoints), `server/homeschoolPlanner.ts` (OpenAI generator), schema in `shared/schema.ts` (`generateHomeschoolPlanRequestSchema`, `HomeschoolActivity`, `HomeschoolDayPlan`, `GeneratedHomeschoolPlan`)
-   **Tamper-Evident Audit Trail**: `server/services/auditLog.ts` (`logAuditEvent` hash chain + `verifyAuditChain`), hash columns on `auditLogs` in `shared/schema.ts`, verify endpoint `GET /api/admin/audit-logs/verify` in `server/routes/admin.ts`
-   **Observability**: `server/observability.ts` (pino logger + http logger + redaction + request id), `/health` + `/ready` + structured error handler in `server/index.ts`
-   **COPPA + GDPR/CCPA DSR + Retention**: `server/services/dataSubjectService.ts` (logic), `server/routes/dsr.ts` (export/delete/admin-list endpoints), COPPA onboarding gate in `server/routes/account.ts`, retention purge scheduler in `server/index.ts`, `data_subject_requests` table + user lifecycle fields in `shared/schema.ts` / `shared/models/auth.ts`
-   **In-Memory Caching**: `server/services/memoryCache.ts` (TTL cache + `cached()` middleware), applied to standards metadata in `server/routes/org.ts`
-   **Dependency / CI Hardening**: `scripts/audit-deps.mjs` (audit gate), `.github/dependabot.yml`, `.github/workflows/ci.yml` (ready-to-enable), tests in `server/__tests__/` (vitest + `vitest.config.ts`)
-   **Team Hub Staff Approval Gate**: Team Hub membership is admin-approved. `staff_access_requests` table (`shared/schema.ts`), pure gate logic in `server/services/teamAccessPolicy.ts` (`canAccessTeamHub`: foundation admins site_admin/system_admin always in, everyone else needs an approved row). `requireApprovedStaff` middleware on all `/api/team/*` reads; access endpoints in `server/routes/teamHub.ts` (`GET /api/team/access/me`, `POST /api/team/access/request`, admin queue `GET/POST /api/team/access/requests[...]` — approve requires fresh MFA). Approving a student/homeschool_parent/educator elevates them to `staff` and records `priorRole`; revoking restores that prior role (never touches roles changed since). Existing staff are grandfathered at boot (`grandfatherExistingStaffAccess` in `server/index.ts`). UI: `useTeamHubAccess` hook gates the sidebar/command-palette group and `TeamHub.tsx` (request/pending/denied card; admin queue in People tab). Tests in `server/__tests__/auth-rbac.test.ts`.
-   **Team Hub (internal HR + ops)**: staff-gated internal hub at `/team` + `/team/:tab`. Schema in `shared/schema.ts` (`hr_roles`, `employees`, `hr_onboarding_tasks`). Seed: `server/hr/roleSeed.ts` merges 1 hand-curated role (`study-abroad-srm`) with the **full 38-role directory** in `server/hr/rolesGenerated.ts` (deduped by stable id). The 38 roles are AUTO-GENERATED from `server/hr/rolesSource.txt` (a `pdftotext -layout` extraction of `attached_assets/LYS_Comprehensive_Roles_*.pdf`) by `scripts/ingest-lys-roles.ts` — re-run `npx tsx scripts/ingest-lys-roles.ts` to refresh (faithful transcription: blank KPI/SOP targets in the source are NOT fabricated). `server/hr/seedHrRoles.ts` (`seedHrRoles`, wired in `server/index.ts` boot) is **idempotent by stable id** — it never overwrites admin edits, so regenerating the file does NOT repair already-seeded rows (delete the `isSeed=true` `role-%` rows + restart to force a clean reseed). Admins add roles and **adjust/increase KPIs + SOPs** via the `RoleFormDialog` editor in `TeamHub.tsx` (KPIs one-per-line `Name :: target`; SOPs as Daily/Weekly/Monthly/Semester/Yearly textareas). Ingest quality is guarded by `server/__tests__/role-ingest.test.ts` (no section/role-header bleed, 38 roles, unique ids). Storage CRUD in `server/storage/teamHub.ts`. Routes in `server/routes/teamHub.ts` (`/api/team/*`; reads `requireStaffOrAdmin`, writes `requireFoundationAdmin`; `GET /api/team/aggregate` backs the operational views — managers see the whole roster, staff see only themselves). UI: `client/src/pages/TeamHub.tsx` (tab shell: Overview, Role Directory, People, My Onboarding, plus folded-in "Our Foundation" frame) + `client/src/pages/TeamHubViews.tsx` (Journey Map, Kanban board, Master Calendar, tri-level Dashboards personal/team/leadership, Customer-Onboarding Training module). Nav group in `client/src/components/AppSidebar.tsx` (`minRole: "staff"`). HR job roles are intentionally SEPARATE from the platform `ROLE_HIERARCHY`.

## Architecture decisions

-   **Zero-Trust Data Governance**: Implemented with 7 rules for data immutability, communication safety, tenant scoping, data residency, and fraud protection.
-   **FERPA Pre-LLM Sanitization**: Free-text fields are stripped of PII before OpenAI processing using a dedicated service (`server/services/piiSanitizer.ts`).
-   **AI Lesson Plan Semantic Retrieval**: Gated by `new_lesson_retrieval` feature flag, uses OpenAI embeddings (cosine-ranked) for more relevant exemplar selection.
-   **LYS Master Teacher Voice Infusion**: Gated by the same `new_lesson_retrieval` flag. `voiceProfileService` injects a static rubric + cosine-retrieved snippets from `lys_canon_entries` (kinds `voice` / `exemplar_full`, seeded from `server/reference/lys/`) into both lesson and assignment prompts. After generation, `voiceCriticService` (gpt-4o-mini) scores the output 0-100 and rewrites if below `VOICE_CRITIC_THRESHOLD` (default 80). Results land in `lesson_generation_attribution` and `assignment_generation_attribution` for the admin Performance panel.
-   **Generation Resilience & Streaming UX**: When OpenAI is unreachable, `server/services/fallbackResolver.ts` runs a cache→exemplar→error cascade (cached prior generation by topic+grade, then top cosine `exemplar_full` canon entry adapted into the output shape, then `GenerationFallbackError`). Successful assignment generations persist `generatedContent` into `assignment_generation_attribution` to seed that cache. Mock template generators are no longer used in production paths. Streaming endpoints `POST /api/lessons/generate-stream` + `POST /api/assignments/generate-stream` (in `server/routes/lessons.ts`) emit SSE phase + delta events consumed by `client/src/lib/streamGeneration.ts` and rendered by `client/src/components/GenerationCountdown.tsx` (5-phase branded countdown + thinking ticker + fallback warning banner). Phase ticks are synthesized server-side via `server/services/generationStream.ts`; OpenAI itself is still called non-streaming to keep the existing regenerate-on-low-quality + voice critic loop intact.
-   **International Standards Fallback**: When a CSP-seeded jurisdiction exists but has zero standard sets attached (Nigeria, Philippines, Indonesia, India, etc.), `server/routes/org.ts` falls through to the static curriculum file in `shared/standards.ts` instead of short-circuiting on jurisdiction existence. `getSubjects` matches African countries by country alone (ignoring per-state abbreviation) and exposes a generic `internationalCoreSubjects` set for non-African developing nations. `LessonGenerator` requires standard-code selection only when the codes endpoint actually returns codes — international curricula without per-outcome codes proceed and let the AI infer outcomes from topic+grade+country context.
-   **Embedded Routes (DO NOT REMOVE)**: `EmbedRouter` (mounted at `/embed/:rest*` in `App.tsx`, lazy-loaded, `/embed/` is in `EXEMPT_PATHS` to bypass the onboarding guard) powers two embed modes used by in-app embedded drawers/modals and external iframes. (1) Single-component embeds at `/embed/<page>` (e.g. `/embed/lesson-generator`) wrap one page in `EmbedWrapper` and auto-report height to the parent via `postMessage({ type: 'lys-resize' })`. (2) Full-site embeds at `/embed/full/*` render the page with `EmbedSidebar` and sync routing across the iframe boundary via `lys-route-change` (child→parent) and `lys-navigate` (parent→child) messages. These are not dead navigation; removing them breaks embedded surfaces.
-   **Role-Aware Navigation & Home (App Simplification)**: A single home at `/` branches by role — students get `StudentDashboard`, everyone else gets `EducatorDashboard` topped with `RoleQuickStart` (role-tailored shortcut band) when authenticated. Anonymous visitors at `/` get a **full-screen, role-routed landing page** (`RoleRoutedLanding`): role cards (Teacher / Student / Homeschool Parent / School Admin) preview tailored headline/proof/benefits/CTA on hover and lock on click; CTAs route teacher/student/parent → `/lesson-generator` and admin → `/pricing`, sign-in → `/api/login`. The landing is rendered **outside** the app sidebar/header/footer shell — gated in `AppShell` (`client/src/App.tsx`) by `!isAuthenticated && location === "/"` — so it reads as a true pre-login marketing surface; all other routes (incl. `/embed/*`) and all authenticated users keep the normal shell. The sidebar (`AppSidebar.tsx`) gates every group/item via `minRole`/`exactRole` against `ROLE_HIERARCHY`; `navigationGroups` + `hasMinRole` are exported and reused by `CommandPalette` (Cmd/Ctrl+K jump-to search) so the two never drift. `/settings` is one role-aware page that links out to admin pages (`/admin`, `/district-admin`, `/system-admin`) rather than duplicating those controls.
-   **Tamper-Evident Audit Trail**: `auditLogs` rows form a SHA-256 hash chain. `logAuditEvent` runs in a transaction guarded by a fixed `pg_advisory_xact_lock` so concurrent writers serialize into one linear chain (no forks); each row commits a deterministic (`stableStringify`) payload + the previous row's hash. `verifyAuditChain()` walks the chain from a `GENESIS` marker, recomputing hashes to detect edits, deletions, forks, and cycles. Exposed via `GET /api/admin/audit-logs/verify`.
-   **Structured Observability**: pino-based structured logging with secret redaction and a per-request `x-request-id` (`server/observability.ts`); `/health` (liveness) + `/ready` (readiness) endpoints; the global error handler logs the stack with the request id and hides 5xx internals from clients. Built-in logging only — no external error-tracking service.
-   **API Versioning**: A URL-rewrite middleware maps `/api/v1/*` → `/api/*` so the canonical `/api/v1` prefix and the legacy `/api` prefix are equivalent (back-compat).
-   **COPPA + GDPR/CCPA Compliance**: Onboarding requires a birthdate for **every** self-serve completion (regardless of declared role) so age is always evaluated, then blocks under-13 self sign-up (`isUnderCoppaAge`); under-13 users may only be added via a school or homeschool-parent account. Self-serve onboarding can only set a **non-privileged** role (`student`/`educator`/`homeschool_parent` — `SELF_SERVE_ONBOARDING_ROLES`); elevated roles are granted only through admin flows (a runtime guard before `updateUserRole` rejects + audits any escalation attempt). Data-subject requests (`/api/dsr/*`) support export (JSON) and erasure: self-serve accounts are **hard-deleted**, school-owned student records are **anonymized**. When the subject is a student, the school admin(s) and parent(s) are alerted (via audit events + DSR `resultDetails`). **Authorization is tenant-scoped**: only `site_admin`/`system_admin` have a global override (`isPlatformAdminRole`); campus/district admins may only act on / list subjects inside their managed org sub-tree (admin/owner memberships only — a plain member role grants no scope), in addition to the subject themselves and active parents/guardians. A daily scheduler runs `runRetentionPurge` (3-year retention via `retentionPurgeAt`).
-   **Full & Real Standards Ingestion (foundational)**: Standards brought in from any authority must be the FULL, REAL published set — never partial samples, never hand-typed, never fabricated. The authoritative path is the Common Standards Project (CSP) sync (`cspService.ts`), which ingests the complete standard tree into `educational_standards`. CSP tags every Texas CTE set with the generic subject `"CTE (2010-)"`; the Ag seed overrides the ingested set's `subject` to a clean teacher-facing label (e.g. `"CTE: Agriculture, Food & Natural Resources"`) so each course surfaces distinctly in `listSubjects`/`listCodes` (which match on `standard_sets.subject`). The LLM/PDF extraction path (`llmExtractionService.ts`) processes the ENTIRE document in line-aligned chunks (no silent 15k-char truncation) and flags incomplete ingestion (failed chunks / size-limit) loudly so partial extractions are caught before publishing. `shared/standards.ts` remains ONLY a labeled last-resort fallback (tagged `source: "fallback"`) to keep dropdowns non-empty for unseeded jurisdictions — it is not an authoritative source.
-   **Texas Agriculture TEKS as a CTE Course**: "Principles of Agriculture, Food and Natural Resources (2024)" — the most current full release — is ingested from CSP for the Texas jurisdiction and surfaced under the subject `"CTE: Agriculture, Food & Natural Resources"` (76 standards, grades 9–12). Re-run `npx tsx scripts/seed-texas-ag-teks.ts` to refresh; add more AFNR courses (Practicum, Ag Mechanics) via the `AG_COURSES` list in `agricultureTeksSeed.ts`.
-   **Student-vs-Teacher Backend Gate (`requireTeacher`)**: Teacher-only feature endpoints are guarded server-side so the `student` role cannot reach teacher functionality (previously these were `isAuthenticated`-only, relying on UI hiding + ownership scoping). Each of `server/routes/lessons.ts`, `classroom.ts`, `student.ts`, `curriculum.ts` defines a local `requireTeacher = requireRole("homeschool_parent","educator","staff","campus_admin","district_admin","site_admin","system_admin")` (matches the existing per-file guard pattern). It blocks ONLY `student` (level 0) — homeschool parents legitimately use lesson/assignment/gradebook/curriculum tools, so they pass. Applied to lesson + assignment create/save/delete/share/assign, class management + gradebook + attendance (incl. teacher **reads**: class `assignments`/`grade-categories`/`grades`/`:id/students` roster/`notes`/`attendance` + `GET /api/class/bkd-insights`), student-record management (create/update/delete/notes/groups/achievements + teacher `student-journey/*` management + list `GET /api/students` + `GET /api/student-journeys`), and scope/unit/sync curriculum authoring. The two per-student reads that had no auth check AND no student-facing caller (`GET /api/students/:studentId/attendance`, `.../achievements`) now use `ensureStudentRecordAccess` (owner/org-admin/site-admin), matching their siblings (`.../grades`, `.../matriculation`). Deliberately NOT applied to student-facing endpoints: `/api/my-journey/*`, `/api/my-assignments`, `/api/practice/*`, `/api/portfolio`, `/api/saved-careers`, `/api/self-discovery`, `/api/strengths`, `/api/goals` (Action Plans), `/api/campus-activities`, and student-self reads `GET /api/students/:id` + `GET /api/student-assignments/:studentId` (consumed by `StudentDashboard` for the signed-in student) and owner-scoped reads with existing checks (`.../grades`, `.../matriculation`, `student-journey/:studentId(/history)`). **Residual**: `GET /api/students/:id` and `GET /api/student-assignments/:studentId` still lack an IDOR check (a logged-in student could read another student's record/assignments by id) — closing this needs student-account↔student-record ownership mapping (the `students.userId` column is the educator owner, not the student's auth id), tracked as a separate hardening follow-up. Boundary documented hermetically in `server/__tests__/auth-rbac.test.ts`.
-   **Multi-Tenancy with Hierarchical Access**: Supports complex organizational structures (School, District, Charter Network) with cascading resources and settings.
-   **Monorepo for Cohesion**: Organized into `/client`, `/server`, and `/shared` to manage related services and frontend.

## Product

-   AI-powered lesson planning and assignment generation.
-   Student practice generator (`/practice`): subject/grade/topic → practice questions with progressive step-by-step hints, answer reveal + explanation. Open to anonymous via the shared guest email-gate + 5/month free quota (reuses `tryReserveGuestLessonGeneration`, so practice + lessons share one guest bucket). Reached from the anonymous landing's Student card.
-   Self-discovery, career exploration, and goal-setting tools (Be-Know-Do framework).
-   Comprehensive educational resources and shared library.
-   Student digital portfolios and transfer system.
-   Gradebook and SIS integration (Clever currently live).
-   Parent Portal with messaging and portfolio oversight.
-   Monetization via marketplace, subscriptions, and affiliate programs.
-   Automated scholarship scraping and mentorship tools.

## User preferences

Preferred communication style: Simple, everyday language.

## Gotchas

-   **OpenAI Credentials**: Replit's managed OpenAI integration provisions `AI_INTEGRATIONS_OPENAI_API_KEY` / `AI_INTEGRATIONS_OPENAI_BASE_URL`. `server/bootstrap-env.ts` (imported first in `server/index.ts`) bridges these to `OPENAI_API_KEY` / `OPENAI_BASE_URL` so existing OpenAI clients work unchanged. If both are missing, generators silently fall back to a typo-laden mock template — always confirm the integration is connected.
-   **`new_lesson_retrieval` Feature Flag**: AI lesson plan semantic retrieval is OFF by default.
-   **SIS Integration**: Only Clever is fully integrated and live; others are "Coming Soon".
-   **LYS Canon Refresh**: After editing/adding `.txt` files in `server/reference/lys/`, run `node scripts/regen_lys_embedded.mjs` to refresh the corpus.
-   **Scholarship Scraper**: Newly scraped scholarships are inactive by default and require system_admin approval.

## Pointers

-   **Developer Execution Plan**: `docs/developer-execution-plan.md`
-   **OpenAI API Documentation**: [https://platform.openai.com/docs/](https://platform.openai.com/docs/)
-   **Drizzle ORM Documentation**: [https://orm.drizzle.team/docs/overview](https://orm.drizzle.team/docs/overview)
-   **Tailwind CSS Documentation**: [https://tailwindcss.com/docs](https://tailwindcss.com/docs)
-   **shadcn/ui Documentation**: [https://ui.shadcn.com/docs](https://ui.shadcn.com/docs)